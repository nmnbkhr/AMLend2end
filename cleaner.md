# Jupyter Kernel Fix

## When kernel keeps "connecting" or "restarting", run:

```bash
./fix_kernel.sh
```

## Then in VS Code:

1. `Ctrl+Shift+P`
2. Type: `Developer: Reload Window`
3. Press Enter
4. Open notebook and select kernel
